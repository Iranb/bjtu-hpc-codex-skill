interface:
  display_name: "BJTU HPC"
  short_description: "Safely migrate accounts and run BJTU HPC tasks"
  default_prompt: "Use $bjtu-hpc to configure a controller, migrate encrypted accounts, inspect queues, or run refresh-gated native Slurm tasks."
